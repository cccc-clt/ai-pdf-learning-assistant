# 复制本文件为 `.streamlit/secrets.toml` 并填入真实密钥（勿提交到 Git）。
# Copy to `.streamlit/secrets.toml` and fill in real values (do not commit).

OPENAI_API_KEY = "sk-..."
OPENAI_MODEL = "gpt-4o-mini"
# 若已在 src/app_settings.py 配置 OPENAI_BASE_URL，此处可省略
# OPENAI_BASE_URL = "https://api.quickrouter.ai/v1"
